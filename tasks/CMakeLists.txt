# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(offshore_pipeline_detectorTaskLib)
ADD_LIBRARY(${OFFSHORE_PIPELINE_DETECTOR_TASKLIB_NAME} SHARED 
    ${OFFSHORE_PIPELINE_DETECTOR_TASKLIB_SOURCES})
add_dependencies(${OFFSHORE_PIPELINE_DETECTOR_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${OFFSHORE_PIPELINE_DETECTOR_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${OFFSHORE_PIPELINE_DETECTOR_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${OFFSHORE_PIPELINE_DETECTOR_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${OFFSHORE_PIPELINE_DETECTOR_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${OFFSHORE_PIPELINE_DETECTOR_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${OFFSHORE_PIPELINE_DETECTOR_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${OFFSHORE_PIPELINE_DETECTOR_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${OFFSHORE_PIPELINE_DETECTOR_TASKLIB_HEADERS}
    DESTINATION include/orocos/offshore_pipeline_detector)

